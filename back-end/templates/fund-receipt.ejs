<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Fund Receipt</title>
    <style>
        body { font-family: Arial; padding: 20px; }
        h2 { text-align: center; }
        .section { margin-bottom: 10px; }
    </style>
</head>
<body>
<h2>Festival Fund Receipt</h2>
<div class="section">
    <strong>Date:</strong> <%= new Date(fund.date).toLocaleDateString() %>
</div>
<div class="section">
    <strong>Received From:</strong>
    <%= fund.type === 'house' ? fund.houseId.ownerName : fund.name %>
</div>
<div class="section">
    <strong>Amount:</strong> ₹<%= fund.amount %>
</div>
<div class="section">
    <strong>Payment Method:</strong> <%= fund.paymentMethod %>
</div>
<div class="section">
    <strong>Reference:</strong> <%= fund.reference || 'N/A' %>
</div>
</body>
</html>
